apiVersion: v1
kind: Service
metadata:
  name: {{ include "kalah-metrics.fullname" . }}
  labels:
    {{- include "kalah-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kalah-metrics.selectorLabels" . | nindent 4 }}
